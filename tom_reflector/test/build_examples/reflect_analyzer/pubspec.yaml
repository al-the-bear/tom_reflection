name: reflect_analyzer
description: Example of using tom_analyzer to generate reflection code for the dart analyzer package.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  analyzer: ^8.4.1
  tom_analyzer:
    path: ../../../

dev_dependencies:
  build_runner: ^2.10.4
  build: ^4.0.3
  lints: ^5.0.0
